---
title: "{{ input.title }}"
summary: "{{ input.summary }}"
status: active
unit: "{{ input.unit }}"
direction: increase
target: "{{ input.target }}"
owners: []
createdAt: "{{ input.createdAt }}"
updatedAt: "{{ input.updatedAt }}"
---

# {{ input.title }}
