---
title: "{{ input.title }}"
summary: "{{ input.summary }}"
source: "{{ input.source }}"
confidence: "{{ input.confidence }}"
owners: []
relatedProduct: []
relatedTasks: []
createdAt: "{{ input.createdAt }}"
updatedAt: "{{ input.updatedAt }}"
---

# {{ input.title }}
