---
title: "{{ input.title }}"
summary: "{{ input.summary }}"
status: planned
version: "{{ input.version }}"
date: "{{ input.date }}"
owners: []
relatedTasks: []
relatedValidation: []
relatedMetrics: []
createdAt: "{{ input.createdAt }}"
updatedAt: "{{ input.updatedAt }}"
---

# {{ input.title }}
