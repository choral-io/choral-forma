---
title: "{{ input.title }}"
summary: "{{ input.summary }}"
type: decision
status: "{{ input.status }}"
synthetic: "true"
engagementKey: ENG-SYN-001
sources: []
relatedTo: []
---

# {{ input.title }}

{{ input.summary }}

## Decision boundary

Record the human-confirmed choice, its reason, and the limits that still require validation.
