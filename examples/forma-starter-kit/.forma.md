---
schemaVersion: 1

workspace:
  name: "Choral Forma Example"
  canonicalLanguage: "en"
  supportedLanguages:
    - "en"
    - "zh-Hans"
  timezone: "UTC"
  logo:
    path: "assets/logo.svg"
    alt: "Choral Forma Example"

runtime:
  values:
    workspaceRoot:
      kind: workspaceRoot
    currentDateTime:
      kind: currentDateTime
    currentUserId:
      kind: gitConfig
      key: user.name
      transform: slugify

imports:
  - ".forma/dashboard.md"
  - ".forma/spaces/*.md"
  - ".forma/views/*.md"
  - ".forma/local/*.yml"
  - ".forma/local/*.md"

guidelines:
  - "guidelines/workspace-operations.md"
  - "guidelines/task-selection.md"

types:
  member:
    kind: entryRef
    source: .forma/spaces/members
    input:
      transform: slugify
  task:
    kind: entryRef
    source: .forma/spaces/tasks
    input:
      transform: slugify
  taskStatus:
    kind: enum
    values:
      - todo
      - ready
      - doing
      - blocked
      - reviewing
      - done
  taskPriority:
    kind: enum
    values:
      - high
      - medium
      - low
---

# Forma Workspace

This file is the Forma workspace entry point. Its frontmatter defines workspace configuration.
