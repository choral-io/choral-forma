---
kind: decision
title: "{{ input.title }}"
summary: "{{ input.summary }}"
status: "{{ input.status }}"
owners: []
reviewers: []
related_to: []
decidedAt: "{{ input.decidedAt }}"
createdAt: "{{ input.createdAt }}"
updatedAt: "{{ input.updatedAt }}"
---

# {{ input.title }}

## Context

## Decision

## Consequences
