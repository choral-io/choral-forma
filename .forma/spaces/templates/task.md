---
schemaVersion: 1
kind: task
scope: "{{ input.scope }}"
title: "{{ input.title }}"
summary: "{{ input.summary }}"
type: "{{ input.type }}"
priority: "{{ input.priority }}"
value: "{{ input.value }}"
module: "{{ input.module }}"
effort: "{{ input.effort }}"
status: "{{ input.status }}"
readiness: "{{ input.readiness }}"
owners: []
assignees: []
reviewers: []
tags: []
blockedBy: []
relatedTo: []
severity: ""
sprint: ""
reportedBy: ""
affectedArea: ""
---

# {{ input.title }}

## Goal

## Sources

## In Scope

## Out of Scope

## Acceptance Criteria
