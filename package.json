{
  "name": "choral-forma",
  "private": true,
  "type": "module",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": ">=11.2.0",
      "onFail": "error"
    },
    "runtime": {
      "name": "node",
      "version": ">=24.11.0 <25",
      "onFail": "error"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/choral-io/choral-forma.git"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "check": "pnpm --recursive --if-present check",
    "format:knowledge": "prettier --write \"knowledge/**/*.{md,mdx,md.tpl,mdx.tpl}\" --no-error-on-unmatched-pattern --log-level warn",
    "check:knowledge": "prettier --check \"knowledge/**/*.{md,mdx,md.tpl,mdx.tpl}\" --no-error-on-unmatched-pattern --log-level warn"
  },
  "devDependencies": {
    "prettier": "~3.8.3",
    "typescript": "~6.0.3"
  }
}
